.venv/
__pycache__/
*.pyc

# La clave del proveedor nunca entra al repositorio.
clave.txt   # forma antigua, un fichero por clave
.env

# La memoria es de quien la escribe, no del repositorio.
memoria.db*
prueba.db*
memoria-prueba.db*

# Los registros de la descarga son de la máquina que la corrió.
*.log

# Índice de CodeGraph: se regenera, no se versiona.
.codegraph/

# Checkpoints del grafo: estado de ejecucion, no del proyecto.
grafo.db*

# Los pesos externos del codificador: se generan, no se versionan (235 MB).
codificador-ligero/

local.db*

# Las variables para el hosting llevan las claves dentro.
variables-northflank.txt
